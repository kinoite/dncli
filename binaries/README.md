> [!CAUTION]
> These dncli binaries are only for x86_64, if your CPU architecture isnt x86_64, DO NOT USE THESE.

> [!NOTE]
> More binaries for architectures for future releases will be made soon.

# Hm? what is this place?
Theese are the binaries for dncli, if your computer cannot compile dncli from source.
To install theese, just simply extract the tar.gz archive and move them to your prefered $PATH

```
sudo mv dncli /usr/bin
```